# Architecture (monorepo workstation)

Application architecture remains in package trees (especially `elektron-digital-twin-foundation/`).

This folder holds **cross-cutting workstation architecture** only:

- EDE bootstrap boundaries  
- How scripts relate to packages  
- Pointers to authoritative ADRs

See `PROJECT_MANIFEST.json` and `docs/cto/ENGINEERING_DECISIONS.md`.
