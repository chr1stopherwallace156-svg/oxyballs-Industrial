# Contracts index

Authoritative capture/EDTS JSON schemas live in their owning repositories/packages.

This folder stores **pointers**, not forks:

| Contract area | Authoritative location |
|---|---|
| Capture iOS contracts | external `elektron-capture-ios/Contracts/` |
| EDTS vendored capture bundle | `elektron-digital-twin-foundation/eae/contracts/capture_ios_0_1_4/` |
| Foundation schemas | `elektron-digital-twin-foundation/schemas/` |

Do not silently duplicate schemas here without a digest + provenance record.
