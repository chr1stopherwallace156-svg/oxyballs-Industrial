# SUPERSEDED kernel proposal schemas

Replaced by Draft 2020-12 schemas at repository `schemas/*.schema.json` (exact-vehicle kernel).

Do not add new proposal JSON here. See `documentation/KERNEL_REVISION_LOG.md`.
