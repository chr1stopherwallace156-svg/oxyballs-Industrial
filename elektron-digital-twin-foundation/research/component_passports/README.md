# Component passports (research drafts)

**Authority:** `ILLUSTRATIVE_ONLY` / research draft  
**Not** activated kernel passports. Frozen schema `schemas/component-passport.schema.json` (rc1) is unchanged.

Active kernel passport for the FL door remains:

- `examples/2019_f450/door-fl-component-passport.example.json` → geometry **`ABSENT`**

Draft IDs under this folder (`EDTS-COMP-FL-*`) are sprint inventory handles only. Part numbers and connector counts are **research claims** pending local parts-catalog / EWD hashes (`SRC-CAND-000007`, `SRC-CAND-000006`).
