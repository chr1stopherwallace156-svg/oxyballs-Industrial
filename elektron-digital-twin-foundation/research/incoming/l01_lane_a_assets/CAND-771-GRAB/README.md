# CAND-771-GRAB — staging

**Status:** `DISCOVERED` / `NOT_ACQUIRED`  
**Target:** `CMPINST-VEH000001-DOOR-FL`

1. Record a real source URL in `source_url.txt` (single line) or in `L1_LANE_A_ASSET_CATALOG.json`.
2. Place licensed STEP/CAD bytes under `source/`.
3. Run: `python3 tools/evidence_acquisition/run_evidence_acquisition.py --candidates CAND-771-GRAB`

Do not invent URLs or hashes.
