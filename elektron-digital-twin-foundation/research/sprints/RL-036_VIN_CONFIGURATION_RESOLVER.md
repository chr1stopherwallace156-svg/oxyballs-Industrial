# RL-036 — VIN configuration resolver (DT-D067)

**Decision:** DT-D067  

Implemented `edts-vin-resolver` with CLI `npm run vin -- <VIN>`, fixture tests, and live opt-in integration. VIN evidence creates candidates only — not engineering-Verified configuration or geometry.
