# Twin Domain Passports (Visual / Engineering)

A single marketplace asset cannot satisfy both domains. Visual (`TWIN-VIS-*`) and Engineering (`TWIN-ENG-*`) registries stay independent to prevent configuration errors.

Numeric LOD/mass/tolerance claims without hashed evidence remain `RESEARCH_CLAIM` / `NOT_EXECUTED`.
