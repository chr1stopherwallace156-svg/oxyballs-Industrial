# Open Items

1. Authoritative Git clone equivalence vs validated ZIP; fill `PHASE_1C_FINAL_VALIDATION.md` digests/evidence.
2. Commit A (validation only) + annotated tag `v1.0.0-phase1c` + remote confirm + GitHub protection.
3. Specs 1–6 final baseline sign-off → then `AUTHORIZED_FOR_IR_0001_EXECUTION`.
4. Execute isolated IR-0001 on device; fill `Research/Spikes/IR-0001/RESULTS.md`.
5. Commit B (Specs 1–3) only **after** remote Phase 1 tag confirmation.
